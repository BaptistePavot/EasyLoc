{% extends 'base.html.twig' %}

{% block body %}
<div class="container h-100">
    <div class="row align-items-center h-100">
        <div class="col-5 mx-auto">
            <div class="jumbotron">

                    <h1 class="row justify-content-center">Connexion impossible</h1>
                    <div class="row justify-content-center">
                        <button onclick="window.location.href='{{ path('home')}}'" class="btn btn-primary">Réessayer</button>
                    </div>
            </div>
        </div>
    </div>

{% endblock %}
